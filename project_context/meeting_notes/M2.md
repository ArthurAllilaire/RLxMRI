# Weekly meeting 2:

Setup:

14 sphere T1 and the action space is:

As there is a sequence block that runs the k-space imaging - roughly know how that works.

Noise: added to observations - 5%. Also jitter T1 and position - probably not enough. Started the action space varying TI, TE, TR and alpha. I guess only really care about TR AND TI. ran with 200k steps PPO-clip.

This led to agent policy collapse

so did delta mape instead:
r_t = MAPE_{t−1} − MAPE_t                 # delta-MAPE
and action is TI, TE, TR

now created a bimodal distribution - 0.15s and 1.3s - this overfits a couple of spheres but ones in the middle have awful T1 estimates. Fitter does give values and no way of knowing uncertainity of estimates - so not really able to adapt.

so running with mean MAPE + amx(MAPE) and adding uncertainity estimate from the T1 fitter

Reward is -mean_MAPE and has previous image

Have set up a T1 plate and RL with gradients - policy currently learned is not ideal.

# After that:

Will ig look at adding in z and making time frame longer. wins: have gotten 100 hours of gpu which should speed up training runs.

more time and high accuracy hopefully - truly adaptive